RHTTP SERVER
Run server with service
--sudo systemctl enable rhttp.service
--sudo systemctl start rhttp.service

IF SERVICE NOT PRESENT THEN,
RUN SERVER LIKE
python rhttp.py

PORT number and configurations are present in rhttp.conf file

